package com.disputeshield.backend.domain;

/** Matches the frontend's `ReadinessLevel` = "HIGH" | "MEDIUM" | "LOW" literal type verbatim. */
public enum ReadinessLevel {
    HIGH,
    MEDIUM,
    LOW
}
